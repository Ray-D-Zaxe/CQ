#include <stdio.h>
int main(){
    int a, b, c = 0, i;
    scanf("%d\n", &a);
    for(i = 0; i < a; i++){
        c *= 10;
        scanf("%d", &b);
        c += b;
    }
    printf("%d", c);
  return 0;
}