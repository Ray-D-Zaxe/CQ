#include <stdio.h>
/* Include other headers as needed */
int fibbon(int a){
    if(a <= 1) return a;
    else return fibbon(a - 1) + fibbon(a - 2);
}
int main()
{
    int a;
    scanf("%d", &a);
    for(int i = a - 1; i >= 0; i--) printf("%d\n", fibbon(i));
    return 0;
}