#include <stdio.h>
/* Include other headers as needed */
int main(){
    int a, b = 0, i = 0, arr[7];
    for(; i < 7; i++)
        scanf("%d ", &arr[i]);
    scanf("\n%d\n", &a);
    i = 0;
    while(i < 7){
        if(arr[i] == a)
            b++;
        i++;
    }
    if(b > 0) printf("\n%d", b);
    else printf("\nNot Present");
    return 0;
}