int power(int num, int pow)
{
    if(pow < 0) return -1;
    else if (pow == 0) return 1;
    else{
        int a = 1, i;
        for(i = 0; i < pow; i++)
            a *= num;
        return a;
    }
}